git command usage:

HTTPS/SSH:
¡­or create a new repository on the command line
  echo "# (your-repository-name)" >> README.md
  git init 
  git add README.md
  git commit -m "(your-custome-message)"
  git remote add origin (https:// or git@)github.com/(your-github-name)/(your-repository-name).git 
  git push -u origin master

¡­or push an existing repository from the command line
  git remote add origin (https:// or git@)github.com/(your-github-name)/(your-repository-name).git 
  git push -u origin master


Remarks: There are brackets to support the need to change your own, remember to change the brackets removed!
